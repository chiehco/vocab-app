# 學習入口簡化｜2026-09-22

## 授權與範圍

使用者確認互動示範，追加「劇情先藏，沒有的關掉」，確認首次使用畫面後明確指示「開始改」。本機驗收完成後，使用者再以「好，推吧」授權提交與發布。

工作樹：`C:/Code/vocab-app-simple-home`；分支：`codex/simple-learning-home`；基底：`origin/main` 的 `f76fe08`。原工作樹及其他尚未提交的內容修改保留。

## 介面與行為

- 使用原有淡底、綠色主要動作與字級 token；以單一開始區、留白與分隔線排列次要入口。保持既有按鈕回饋及低動態偏好，不加入裝飾動畫。
- 首頁沒有有效的接續位置時顯示「選課本單元」；有效字卡位置或尚未完成的教材題目顯示「繼續學習」。數字表示閱讀／作答位置，不宣稱字卡已熟記。
- 已到期且非 new 狀態的可用單字才顯示「今日複習」。首頁數量與 `/review?due=1` 使用同一資料函式；不插入新字或同日回顧。其他複習入口原有排程行為保留。
- 底部四入口：首頁、單字、大考、遊戲。單字直達 `/textbook`，可另進全部單字及群組。劇情及尚未開放的會考／聽音／看圖入口隱藏，舊 `/story` 書籤回首頁。
- 課本單元只列實際可用的等級／Unit：LV1 已審閱圖句及 LV3／LV4 教材。部分 LV1 內容有明確標示；不把 30 字自動分組冒充課本單元。
- 選 Unit 後直接「看字卡／做題目」，建立群組放在更多選項。LV1 圖句依本 Unit 顯示精確的圖片及中英圖說，補充詞不虛構 wordId；LV1 做題使用正式詞條的一般看英文選中文，10 題直接開練。LV3／LV4 沿用正式教材題庫，每輪先取 10 題。
- 一般單字工作區按鈕改「看字卡／做題目」，教材題目返回連結保留實際單元，避免跳回預設 LV3。

## 資料界線與接續能力

閱讀位置新增於既有 settings 的 `learning:last-location`，不升級或刪除資料庫；既有備份機制可保留此 setting。支援一般字卡／篩選字卡清單／群組字卡、30 字分組、教材字卡與 LV1 圖句。使用 ID 驗證現有內容；失效清單、已刪字卡不產生失效接續入口。另依既有 directSessions 接續未完成教材作答，保留已提交答案。

舊版沒有保存的閱讀位置無法追溯，會從這版開始記住。一般 Quiz、遊戲與學測整卷原本的作答中途保存能力未擴張；LV1 一般單字練習仍沿用 Quiz，不能把本次接續功能宣稱為所有玩法都可精確續答。

沒有更動主 Excel、圖片、例句、字義、SRS 演算法或現有學習紀錄；驗收用資料只寫入隔離測試瀏覽器。

## 驗證

- 新增 8 項實質測試：新使用者、閱讀位置及備份還原、失效位置、LV1 補充詞、無群組清單、未完成／完成作答、最新活動選擇、到期佇列讀取不改 SRS。
- 全套 54 檔、261 測試通過；lint 無警告，TypeScript／production build 通過。建置維持既有大型 bundle 提示。
- 第一輪全套與建置並行時，一項既有 726 圖雜湊驗證超過預設 5 秒；限制測試 workers 後全套通過，未放寬斷言或修改該測試。
- 隔離 Playwright：空進度首頁、LV1 第二張經重載及首頁接續、LV4 第二張接續、直接開始 10 題、首題作答後第二題重載（1 筆作答紀錄，0 個自建群組）、LV1 一鍵開題、到期 1 字的首頁與實際 1／1 複習、移到未到期後首頁隱藏入口。
- 320／390／1280px，首頁 130% 字級無水平溢出；手機首頁與單元開始頁目視確認。大考／遊戲無準備中入口，舊劇情路由回首頁，瀏覽器 error／warning 均 0。
- 螢幕截圖位於 `output/playwright/`。以上為發布前本機驗收；正式部署結果另記工作區 D-086 補記。實體手機尚未驗收。
